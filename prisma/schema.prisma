generator client {
  provider = "cargo prisma"
  output   = "./src/lib.rs"
}

datasource db {
  provider = "mysql"
  url      = env("DATABASE_URL")
}

model Costumer {
  id    Int     @id @default(autoincrement())
  email String  @unique
  name  String?
}
